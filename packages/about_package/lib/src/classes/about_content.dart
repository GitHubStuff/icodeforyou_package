import 'package:flutter/material.dart';

class AboutContent {
  const AboutContent({
    required this.title,
    required this.demo,
    required this.size,
  });

  final Widget title;
  final Widget demo;
  final Size size;
}
